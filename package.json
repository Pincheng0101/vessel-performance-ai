{
  "name": "ym-datalake-poc",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK CLI toolkit for the ym-datalake-poc project (Python app; see pyproject.toml for the CDK library).",
  "scripts": {
    "cdk": "cdk"
  },
  "devDependencies": {
    "aws-cdk": "^2.1000.0"
  }
}
