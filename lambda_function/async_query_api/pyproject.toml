[project]
name = "async-query-api"
version = "0.1.0"
description = ""
requires-python = ">=3.13, <3.14"
dependencies = [
    "aws-lambda-powertools",
    "pydantic>=2.0.0,<3.0.0",
]

[tool.uv]
package = false
