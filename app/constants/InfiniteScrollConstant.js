// Status strings accepted by Vuetify's `v-infinite-scroll` `@load` `done()` callback.
const LoadStatus = Object.freeze({
  OK: 'ok',
  EMPTY: 'empty',
  ERROR: 'error',
  LOADING: 'loading',
});

export {
  LoadStatus,
};
