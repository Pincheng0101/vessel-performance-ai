const ViewMode = Object.freeze({
  MODE_FORM: 'form',
  MODE_JSON: 'json',
  MODE_MARKDOWN: 'markdown',
});

const SECRET_INPUT_PLACEHOLDER_ID_PREFIX = 'secret_input_placeholder';

export {
  SECRET_INPUT_PLACEHOLDER_ID_PREFIX,
  ViewMode,
};
