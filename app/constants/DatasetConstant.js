const DefaultParams = Object.freeze({
  FILTER_UNDEFINED_FIELDS: false,
});

const FieldType = Object.freeze({
  INPUT_FIELD: 'inputField',
  OUTPUT_FIELD: 'outputField',
});

export {
  DefaultParams,
  FieldType,
};
