const IgnoredValidationKeywords = Object.freeze([
  'if',
  'then',
  'anyOf',
  'oneOf',
  'not',
]);

const SeverityType = Object.freeze({
  ERROR: 'error',
});

export {
  IgnoredValidationKeywords,
  SeverityType,
};
