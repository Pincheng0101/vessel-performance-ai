const DefaultParams = Object.freeze({
  CATEGORY: 'all',
  FILTER_LOGIC: 'AND',
  FILTERS: undefined,
  PAGE: 1,
  PER_PAGE: 10,
});

const ItemsPerPageOption = Object.freeze({
  LIST: [10, 20, 50, 100, 200],
});

export {
  DefaultParams,
  ItemsPerPageOption,
};
